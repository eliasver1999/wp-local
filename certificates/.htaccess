# Deny all direct web access to wallet credentials (Apache).
# These files are private signing keys — they must never be downloadable.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
